
- `.[1]` for first (Z)
- `.*` for rest (Z)
- `.` for all-of (Z)

- `.[1].f` for  `first (locals[0]).f`
- `.1.f` for `first (locals[0]).f`
- `.f` for `first (locals[0]).f`

- `.` for first (Z)
- `*` for rest (Z)
- `$` for all-of (Z)

